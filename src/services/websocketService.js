const notifyUser = (userId, data) => {
    console.log(`\n[STUB] Waiting to send WebSocket to user: ${userId}`);
    console.log(`[STUB] Data:`, JSON.stringify(data));
};
module.exports = {
    notifyUser
};
